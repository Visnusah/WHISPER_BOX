import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById
    ('root'));
root.render(
    <App />  // this is the root component (jsx -> jsx is javascript xml which is a combination of javascript and html)
);
